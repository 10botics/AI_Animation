# =============================================================================
# Fal.ai API key — edit this file (safe to commit only while FAL_KEY= is empty)
# =============================================================================
# Paste your key after FAL_KEY= (no quotes):
#   https://fal.ai/dashboard/keys
#
# Or put secrets in `.env.local` (gitignored) — it overrides this file.
# Never commit a real key to GitHub.
# =============================================================================

FAL_KEY=

# Optional — AI Animation Studio dashboard port (default 8501)
# STUDIO_PORT=8502

# Optional — default chapter folder under Comic Source/ for this instance
# STUDIO_CHAPTER=Chapter-81
